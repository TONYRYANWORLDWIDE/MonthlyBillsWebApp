@model MonthlyBillsWebApp.ViewModels.ViewModelMonthlyWeekly

@*@model IQueryable<MonthlyBillsWebApp.ViewModels.ViewModelMonthlyWeekly>*@

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div>
    <h4>ViewModelMonthlyWeekly</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>

<div>
    <span>
        @foreach (var item in Model.weeklyBills.ToList())
        {
        <span>
            @Html.DisplayFor(modelItem => item.Bill)
        </span>
        }

    </span>
        @*@foreach (MonthlyBillsWebApp.ViewModels.ViewModelMonthlyWeekly mw in Model)
        {
    <span>
        @Html.EditorFor(model => @mw.monthlyBills)
    </span>
    <span>
        @Html.EditorFor(model => @mw.weeklyBills)
    </span>
        }*@

</div>
